# p2p
on GitHub code space: (بعد تحميل فلاتر)
1. ```export PATH="$PATH:/workspaces/p2p/flutter/bin"```

2. ```flutter analyze lib```

3. ```flutter run -d web-server --web-port 8080 --web-hostname 0.0.0.0```
